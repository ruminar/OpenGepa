# リリースノート

## 0.4.0

- 通常アプリランチャー／Webランチャーのタブ、Group、FileItem、DirectoryItem、UrlItemへ、最大4,000文字の説明を追加
- 説明を編集画面とMCPの双方から表示・編集でき、Profileでも移行可能に変更
- 公式C# SDK 2.2.0とSTDIOを使うローカルMCPサーバー `OpenGepa.Mcp.exe` を追加
- MCPからタブ・項目の一覧、階層参照、名前・説明検索、詳細取得、起動履歴、使用頻度を参照可能に変更
- MCPから既存の許可項目を起動可能にし、任意パス／URL／コマンドの直接実行と危険なWindows主要操作を禁止
- MCPからの説明更新を全件検証・単一保存とし、一件でも不正または保存失敗なら全件を変更しないように実装
- MCPの `ref` とページングcursorを推測困難なセッション内トークンとし、別セッション、改変、失効後の利用を拒否
- OpenGepaのMutex、表示通知、MCPパイプを配置ディレクトリ単位に分離し、別配置のポータブルコピーを同時実行可能に変更
- `opengepa.json`をformatVersion 4、ProfileをformatVersion 3へ更新し、従来データを互換読込

## 0.3.1

- Windows Menuの`.lnk`ショートカットを、通常アプリランチャーの編集画面へドラッグ＆ドロップで登録できるように変更
- ドロップしたショートカットは通常のFileItemとしてコピーし、元のWindows Menu項目は変更せず、以後も同期しない
- 起動履歴、使用頻度、ストアアプリの利用可能な項目も編集画面へドラッグ登録可能に変更。履歴・頻度を削除しても登録済み項目は維持
- 起動履歴の各行から、その項目の履歴だけを削除可能に変更。利用不可の行にも対応し、使用頻度は維持
- 履歴行の右クリックでWPFの文字要素を押した場合にアプリが終了する不具合を修正
- FileItemの右クリックから、`.lnk`の実体参照とOpenGepa管理ショートカットへの変換を追加。変換時はアイコンを自動で再取得し、引数または異なる作業フォルダが失われる時は警告して続行を確認

## 0.3.0

- 起動履歴タブを追加。同じ日に同じ項目を複数回起動した場合は一行へまとめ、最終時刻と回数を表示
- 使用頻度タブを追加。履歴の保持上限とは独立して［過去30日］と［全期間］を集計
- 項目ごとの［起動記録に残さない］、除外解除、履歴削除、頻度リセットを追加
- 通常アプリランチャーとWebランチャーへ、コンパクトなツリー内区切り線を追加。通常表示では`Ctrl`を押している時だけ選択・並び替え可能
- ブックマークHTMLの`<HR>`を区切り線として入出力し、ルートまたは任意のGroupを取込先として選べるように変更
- アプリランチャー／Webランチャーを設定画面から削除できるように変更。削除対象と復元不可であることを確認画面へ明記
- 縦タブにスクロールバーが必要な場合だけタブ列を広げ、ウインドウ幅を変えずにアイコンと名前の表示領域を確保
- 起動記録を`opengepa.usage.json`へローカル保存し、Profileと初期データから除外
- `opengepa.json`をformatVersion 3、ProfileをformatVersion 2へ更新し、v0.2.0の保存データとProfileを互換読込
- ファイル入出力を行うブックマークとProfileのメニュー・ボタンへ［📥］［📤］を追加
- アプリケーション全体を対象とする汎用Undo／Redoを採用しない方針を明文化

## 0.2.0

- Windows Menu、ストアアプリ、Web専用、Windows主要操作のシステムタブを追加
- WebランチャーのブックマークHTMLインポート／エクスポート、HTMLまたはWebからのサイトアイコン取得を追加
- ストアアプリを15件目安の先頭文字Groupへ整理し、バックグラウンド更新・実行中キャッシュ・［すべて展開］に対応
- Windows主要操作へ、GPU管理、デバイス・ネットワーク管理、電源／セッション、メディアコントロールを追加。環境依存項目は利用可能時だけ表示
- 通常ランチャーからOpenGepa管理ショートカットを作成し、Profileで移行できるように変更
- 設定画面を［一般］［外観とアイコン］［ランチャータブ設定］［Windows機能］［データ］へタブ化。Windows Menu編集許可、表示位置モード、固定プリセットの表示／復帰を追加
- トレイ操作を高速化し、連続クリックによる表示切替の蓄積を抑制
- 既存の0.1.x設定は自動移行し、最初の起動だけ`opengepa.default.json`を初期値として使用

## 0.1.0 — 初回公開

OpenGepa 0.1.0は、Windowsのタスクトレイに常駐する個人向けランチャーの初回公開版です。アプリ、ファイル、フォルダ、Webページを複数のランチャーへ整理し、必要なときにすばやく開けます。

### 配布と動作環境

- Windows 11 x64向け
- `win-x64`のフレームワーク依存型ZIP配布
- .NET 10 Windows Desktop Runtime x64が必要
- 日本語UIのみ

### 主な機能

- タスクトレイ常駐、単一起動、トレイ左クリックによるランチャー表示／非表示
- 複数のApp Launcherと、右側の縦タブによる切り替え
- ランチャーの新規登録、編集、複製、表示／非表示、並び替え、アイコン変更
- Groupによる任意深度のツリー整理
- FileItem、DirectoryItem（UNC可）、UrlItem（HTTP／HTTPS）の登録と起動
- 項目種別ごとのシングルクリック／ダブルクリック起動設定
- 表示名検索、キーボード操作、ピン止め、すべて折りたたむ操作
- ツリー内外のドラッグ＆ドロップ、ブラウザURLのドロップ登録
- フォルダ再帰走査、登録プレビュー、進捗表示、同名競合の事前検出
- 実行ファイル、ファイル、フォルダ、URLのアイコン取得・変更・再取得・既定への復帰
- URLのページタイトル設定と、サイトアイコン取得
- ライト／ダーク／カスタムテーマ
- 起動項目・既定アイコン・トレイアイコンなどをアプリディレクトリへポータブルに保存
- 即時保存、バックアップ、最後に正常起動できたデータによる自動復旧
- `.ogp` Profileの保存と読み込み
- Startupフォルダを使ったWindows自動起動の切り替え

### 保存形式

アプリの隣に`opengepa.json`、`opengepa.backup.json`、`opengepa.lastgood.json`、`icon/`、`iconSet/`を保存します。ZIPの展開フォルダをそのままコピーすれば、設定とアイコンをまとめて移行できます。

### 既知の制約

- Dashboardの実コンテンツ、Windows Menu、Web専用タブ、ツリー内の区切り線、Undo／Redoは0.1.0の対象外です。
- サイトアイコンの取得はWebサイト側の公開方法、TLS、リダイレクト、アクセス制限などの影響を受けます。取得できない場合は手動アイコンを設定してください。
- Profileを読み込むと現在のランチャー設定を置き換えます。Profileには起動対象への参照を含むため、信頼できるファイルだけを読み込んでください。
- Windows自動起動の設定はPC固有であり、Profileには保存されません。

### リリース作業の範囲

このリリースノートは`0.1.0`タグ作成前の配布準備として追加しています。タグ作成、mainへの反映、GitHub Releaseの作成は含みません。
